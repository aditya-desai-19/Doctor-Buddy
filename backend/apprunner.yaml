version: 1.0
runtime: nodejs18

build:
  commands:
    - npm install
    - npm run gen
    - npx prisma generate
    - npx prisma migrate dev --name init
    - npm run build

start:
  command: npm start

environment:
  variables:
    DB_URL: "postgresql://postgres:mypassword@5432:5432/doctorBuddy"